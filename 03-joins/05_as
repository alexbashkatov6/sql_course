SELECT COUNT(*) AS employees_count
FROM employees

SELECT category_id, SUM(units_in_stock) AS units_in_stock
FROM products
GROUP BY category_id
ORDER BY units_in_stock DESC
LIMIT 5

! in havung aliases dont work